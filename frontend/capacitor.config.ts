import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plantita.app',
  appName: 'frontend',
  webDir: 'dist/frontend',
  bundledWebRuntime: false
};

export default config;
